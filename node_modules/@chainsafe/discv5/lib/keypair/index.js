import { ERR_TYPE_NOT_IMPLEMENTED } from "./constants.js";
import { Secp256k1Keypair } from "./secp256k1.js";
export * from "./types.js";
export * from "./secp256k1.js";
export function generateKeypair(type) {
    switch (type) {
        case "secp256k1":
            return Secp256k1Keypair.generate();
        default:
            throw new Error(ERR_TYPE_NOT_IMPLEMENTED);
    }
}
export function createKeypair(init) {
    switch (init.type) {
        case "secp256k1":
            return new Secp256k1Keypair(init.privateKey ? init.privateKey : undefined, init.publicKey ? init.publicKey : undefined);
        default:
            throw new Error(ERR_TYPE_NOT_IMPLEMENTED);
    }
}
//# sourceMappingURL=index.js.map